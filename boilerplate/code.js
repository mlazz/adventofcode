const { openFile, splitLines } = require('../../utils');
const inputFile = process.env.INPUT || 'input';
const file = inputFile + '.txt';

const part1 = input => {
    const lines = splitLines(input);
};

const part2 = input => {
    const lines = splitLines(input);
};

openFile(file, part1, part2);